import {StyleSheet} from 'react-native';

const getStarredStatisticStyles = () =>
  StyleSheet.create({
    container: {
      flexDirection: 'row',
    },
  });

export default getStarredStatisticStyles;
